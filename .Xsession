#!/bin/bash

# Load resources
xrdb -merge .Xresources
xmodmap ~/.xmodmap

# icon tray
stalonetray &

# Set background image with feh
feh --bg-scale ~/.xmonad/desktop-background.png &

if [ -x /usr/bin/nm-applet ] ; then
   nm-applet --sm-disable &
fi

exec xmonad
